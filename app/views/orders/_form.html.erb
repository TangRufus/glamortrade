<%= simple_form_for @order_form, wrapper: :horizontal_form, :html => {:class => 'form-horizontal' } do |f| %>
<%= f.input :variant_id %>
<%= f.input :unit %>
<%= f.input :amount %>
<%= f.button :submit, class: "btn btn-lg btn-block btn-effect-ripple btn-primary" %>
<% end %>
